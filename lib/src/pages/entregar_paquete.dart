import 'package:flutter/material.dart';

class EntregarPaquetePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Entregar un paquete'),
      ),
      body: Center(
        child: Text('Hola EntregarPaquetePage'),
      ),
    );
  }
}
