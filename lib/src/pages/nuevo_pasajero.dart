import 'package:flutter/material.dart';

class NuevoPasajeroPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pasajero'),
      ),
      body: Center(
        child: Text('Hola NuevoPasajeroPage'),
      ),
    );
  }
}
